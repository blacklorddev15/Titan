# ── Optional Neon database (same shared DB used by the other bots) ──
# Set DATABASE_URL (or NEON_DATABASE_URL) to enable premium + session sync.
# Leave empty to keep the bot working on local JSON files only.
DATABASE_URL=
NEON_DATABASE_URL=

# ── Bot ──
TELEGRAM_TOKEN=
OWNER_TELEGRAM_ID=7567336073
SUDO_NUMBER=254716951223
BOT_NAME=𝑻𝑰𝑻𝑨𝑵 𝑨𝑵𝑰𝑴𝑬 𝑴𝑫
MENU_IMG=

# ── Optional GitHub session backup (diegdevoff/database) ──
GITHUB_TOKEN=
